name: Deploy React App to Hostinger

on:
  push:
    branches: [ "main" ]   
# or "master" or "deploy"
jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install Dependencies
        run: npm install

      - name: Build Project
        run: npm run build

      - name: Deploy to Hostinger via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          server: 217.196.55.155
          username: u629132086.icamse2027.com
          password: kMAK7syq9mdH2o?T
          local-dir: dist/
          server-dir: ./
          exclude: |
              **/.htaccess